# Activate this profile with SPRING_PROFILES_ACTIVE=env.
# Configure MongoDB through MONGODB_URI or the credential variables below.
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/tvmaze}
spring.data.mongodb.username=${MONGODB_USERNAME:}
spring.data.mongodb.password=${MONGODB_PASSWORD:}
