# Activate with SPRING_PROFILES_ACTIVE=env.
# Credentials remain outside the repository in D:/Pinwox/atlas-credentials.env.
spring.mongodb.uri=${MONGODB_URI}
spring.mongodb.database=${MONGODB_DATABASE:tvmaze}
spring.mongodb.username=${MONGODB_USERNAME:}
spring.mongodb.password=${MONGODB_PASSWORD:}
